<?php

for ($i = 0; $i < $cnt; $i++) {
                    $quantity = $historyResult[$i]["quantity"];
                    if (is_null($historyResult[$i]["in_price"])) {
                        $historyResult[$i]["in_price"] = 0;
                    }
                    
                    
                    if (is_null($historyResult[$i]["in_name"])) {
                        $historyResult[$i]["in_name"] = "No extra ingredient!";
                    }

                        if ($historyResult[$i]["name"] !== $historyResult[$i - 1]["name"]) {
                            $total += ($historyResult[$i]["price"] * $quantity );
                            $total += ($historyResult[$i]["in_price"] * $quantity);
                        } else {
                            $total += ($historyResult[$i]["in_price"] * $quantity);
                        }
                    
                }